/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Hide the floating Next.js logo/dev indicator (bottom-left round badge).
  devIndicators: false,
};

export default nextConfig;
